<h1>Offers</h1>

<% offers.each do |offer| %>
  <div class="offer">
    <p><%= offer['title'] %></p>
    <img src="<%= offer['thumbnail']['lowres'] %>" alt="offer" />
    <p><%= offer['payout'] %></p>
  </div>
<% end %>
